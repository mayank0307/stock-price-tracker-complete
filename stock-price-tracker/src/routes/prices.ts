// // backend/routes/prices.js
// import express from 'express';
// import  Price  from '../models/Price'; // Adjust the path according to your project structure

// const router = express.Router();

// // router.get('/prices', async (req, res) => {
// //   try {
// //     const prices = await Price.find().sort({ timestamp: -1 }).limit(5); // Fetch the most recent 20 records
// //     res.json(prices);
// //   } catch (error) {
// //     res.status(500).json({ message: 'Error fetching prices' });
// //   }
// // });

// export default router;
